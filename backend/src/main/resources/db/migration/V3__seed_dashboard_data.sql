-- Dashboard seed data placeholder
-- V1 dashboard data is served as in-code seed data from DashboardService.
-- No dashboard-specific tables are created in V1.
-- Future versions will add:
--   dashboard_sdlc_snapshot    — periodic SDLC stage health snapshots
--   dashboard_metric_snapshot  — aggregated metric snapshots
--   dashboard_activity_log     — recent activity log entries
-- See docs/04-architecture/dashboard-data-model.md §4 for full schema.
SELECT 1;
